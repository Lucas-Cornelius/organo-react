import './Time.css';

const Time = (props) => {
    return (
        <section className='card'>
            <h3>{props.nome}</h3>
            <div></div>
        </section>
    );
};

export default Time;